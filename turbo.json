{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": [".env"],
  "globalEnv": [
    "NODE_ENV",
    "DATABASE_URL",
    "SUPABASE_URL",
    "SUPABASE_ANON_KEY",
    "SUPABASE_SERVICE_ROLE_KEY",
    "SUPABASE_JWT_SECRET",
    "SUPABASE_STORAGE_BUCKET",
    "ANTHROPIC_API_KEY",
    "NEXT_PUBLIC_API_URL",
    "CORS_ORIGIN"
  ],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**", "!.next/cache/**"]
    },
    "dev": {
      "dependsOn": ["^build"],
      "cache": false,
      "persistent": true
    },
    "lint": {
      "dependsOn": ["^build"]
    },
    "typecheck": {
      "dependsOn": ["^build"]
    },
    "test": {
      "dependsOn": ["^build"],
      "outputs": ["coverage/**"]
    },
    // The API apps are compiled by Vercel's Node runtime from `api/index.ts`,
    // so their `build` is a typecheck and emits nothing.
    "api-student#build": { "dependsOn": ["^build"], "outputs": [] },
    "api-admin#build": { "dependsOn": ["^build"], "outputs": [] },
    "api-student#test": { "dependsOn": ["^build"], "outputs": [] },
    "api-admin#test": { "dependsOn": ["^build"], "outputs": [] },
    "clean": {
      "cache": false
    }
  }
}
