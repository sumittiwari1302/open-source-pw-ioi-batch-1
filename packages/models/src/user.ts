/**
 * Owner: Team 03 — Auth & Identity.
 *
 * Re-exports the profiles table from the central schema. Import as:
 *   import { profiles } from '@repo/models/user'
 */
export { profiles } from './schema'
